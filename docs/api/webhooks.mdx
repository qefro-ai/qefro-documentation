---
title: "Webhooks"
description: "Inbound webhooks Qefro consumes — Razorpay billing (including payment.failed) and WhatsApp Meta Cloud API verification and messages."
sidebar_label: "Webhooks"
---

import {
  InfoBox,
  Warning,
  RelatedTopics,
  FaqAccordion,
  WorkflowCard,
} from '@site/src/components';

# Webhooks

Qefro **consumes** webhooks from billing and messaging providers. Configure secrets in the Admin Console and always verify signatures / challenges.

## Short definition (citation-ready)

> Qefro billing webhooks verify Razorpay signatures and update entitlements; WhatsApp webhooks verify Meta challenges and ingest inbound customer messages into a bound workspace.

## Billing (Razorpay)

| Item | Detail |
| --- | --- |
| Endpoint | `POST /api/v1/billing/webhook` |
| Auth | Razorpay signature verification |
| Important events | `payment.captured`, `payment.failed`, subscription lifecycle |
| Failures | Stored with `error_code` / `error_reason` / `error_description` when present |
| Idempotency | Order-level guards against duplicate captures |

Enable `payment.failed` in the Razorpay Dashboard webhook settings or failures may not appear in-product.

## WhatsApp (Meta Cloud API)

| Item | Detail |
| --- | --- |
| Verify | `GET /api/v1/whatsapp/webhook` |
| Inbound | `POST /api/v1/whatsapp/webhook` |
| Config | Encrypted tokens + workspace binding in Admin Console |
| Plan | Growth+ |

Guide: [Deploy WhatsApp AI](/docs/guides/deploy-whatsapp-ai).

## Architecture

```mermaid
flowchart TB
  RZ[Razorpay] -->|signed POST| Billing[/billing/webhook]
  Meta[Meta] -->|verify + POST| WA[/whatsapp/webhook]
  Billing --> Entitlements[Plan entitlements]
  WA --> WS[Support workspace chat]
```

## Workflow

<WorkflowCard
  title="Operate webhooks safely"
  steps={[
    {title: 'Copy endpoint URLs from docs/console', description: 'HTTPS only.'},
    {title: 'Set secrets in provider + Qefro', description: 'Never commit them.'},
    {title: 'Send a test event', description: 'Confirm 2xx and in-app effect.'},
    {title: 'Monitor failures', description: 'payment.failed + Meta delivery errors.'},
    {title: 'Rotate secrets on staff change', description: 'Update both sides.'},
  ]}
/>

<Warning>
Do not expose unverified webhook handlers. Signature/challenge failures must 4xx — never process the body first.
</Warning>

## FAQ

<FaqAccordion
  items={[
    {
      question: 'Can I push outbound webhooks from Qefro to my server?',
      answer:
        'Prefer Business Tools for request/response integrations. Outbound event webhooks may expand later — check Release Notes.',
    },
    {
      question: 'Why is my plan not upgrading after payment?',
      answer:
        'Confirm Razorpay webhook delivery, signature secret, and that captured events reached /billing/webhook. Check Billing UI and support.',
    },
  ]}
/>

## Related topics

<RelatedTopics
  topics={[
    {label: 'WhatsApp', to: '/docs/platform/whatsapp'},
    {label: 'Deploy WhatsApp AI', to: '/docs/guides/deploy-whatsapp-ai'},
    {label: 'REST APIs', to: '/docs/api/rest-apis'},
    {label: 'Error Codes', to: '/docs/api/error-codes'},
    {label: 'Security Overview', to: '/docs/security/overview'},
  ]}
/>
