---
title: "Rate Limits"
description: "Auth, widget, WebSocket, and tool-test rate limits that protect api.qefro.com from abuse — how to handle HTTP 429."
sidebar_label: "Rate Limits"
---

import {
  InfoBox,
  Warning,
  RelatedTopics,
  FaqAccordion,
  WorkflowCard,
} from '@site/src/components';

# Rate Limits

**Rate limits** protect authentication and runtime surfaces from credential stuffing and runaway clients.

## Short definition (citation-ready)

> Qefro applies per-route rate limits (especially auth and widget/chat paths). Clients should honor HTTP 429 with exponential backoff and avoid parallel login storms.

## Areas (implementation-oriented)

| Area | Behavior |
| --- | --- |
| Login / OTP / forgot / reset | Per-email Redis-style limits (e.g. login on the order of tens per 15 minutes) |
| Widget HTTP | Middleware limits on widget routes |
| WebSocket chat | Message-rate checks (on the order of tens per minute) |
| Tool test endpoints | Limited to prevent abuse of egress |

Exact numbers can change — treat the table as order-of-magnitude guidance and design clients defensively.

## Client guidance

| Do | Don't |
| --- | --- |
| Back off on 429 | Hammer `/auth/login` in a loop |
| Cache JWTs until expiry | Mint a new session per API call |
| Batch admin scripts politely | Parallelize thousands of tool tests |
| Reuse one widget connection | Open unbounded WebSockets per page |

## Workflow

<WorkflowCard
  title="Handle 429 in production"
  steps={[
    {title: 'Detect 429 + Retry-After if present', description: 'Sleep before retry.'},
    {title: 'Exponential backoff + jitter', description: 'Cap total retries.'},
    {title: 'Reduce concurrency', description: 'Especially for auth and tool tests.'},
    {title: 'Escalate if persistent', description: 'May be misbehaving client or shared NAT.'},
  ]}
/>

<Warning>
If you are load-testing, coordinate with support. Synthetic auth floods look like attacks.
</Warning>

## FAQ

<FaqAccordion
  items={[
    {
      question: 'Are plan quotas the same as rate limits?',
      answer:
        'No. Plan quotas (messages/documents) are entitlement limits. Rate limits are abuse controls on HTTP/WS routes.',
    },
    {
      question: 'Do Business Actions to my API count toward Qefro rate limits?',
      answer:
        'Egress calls are separate from your API’s limits. Still design your API for bursty assistant traffic.',
    },
  ]}
/>

## Related topics

<RelatedTopics
  topics={[
    {label: 'Error Codes', to: '/docs/api/error-codes'},
    {label: 'API Authentication', to: '/docs/api/authentication'},
    {label: 'Examples', to: '/docs/api/examples'},
    {label: 'Website Widget', to: '/docs/platform/website-widget'},
    {label: 'Secure Business Actions', to: '/docs/guides/secure-business-actions'},
  ]}
/>
