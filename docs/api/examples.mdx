---
title: "Examples"
description: "cURL, TypeScript, and Python examples against production hosts."
sidebar_label: "Examples"
---

import {
  InfoBox,
  Warning,
  RelatedTopics,
  FaqAccordion,
  WorkflowCard,
  ApiEndpointCard,
} from '@site/src/components';

# Examples


**Examples** for common calls.

## Health

```bash
curl -sS https://api.qefro.com/health
```

## List plans

```bash
curl -sS -H "Authorization: Bearer $USER_JWT" \
  https://api.qefro.com/api/v1/billing/plans
```

## Upload a document

```bash
curl -sS -X POST -H "Authorization: Bearer $USER_JWT" \
  -F "file=@faq.pdf" -F "workspace_id=$WORKSPACE_ID" \
  https://api.qefro.com/api/v1/documents
```

## TypeScript widget identify

```typescript
widget.identify({
  id: user.id,
  email: user.email,
  auth: { mode: 'jwt', token: userJwt },
});
```

## Python plans

```python
import os, urllib.request
req = urllib.request.Request(
    "https://api.qefro.com/api/v1/billing/plans",
    headers={"Authorization": f"Bearer {os.environ['QEFRO_TOKEN']}"},
)
print(urllib.request.urlopen(req).read().decode())
```

## Related topics

<RelatedTopics topics={[
  {label: 'REST APIs', to: '/docs/api/rest-apis'},
  {label: 'Authentication', to: '/docs/api/authentication'},
]} />


