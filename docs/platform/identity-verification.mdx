---
title: "Identity Verification"
description: "Channel-aware authentication for Business Actions — public, verified_channel, and organization_challenge."
sidebar_label: "Identity Verification"
---

import {
  InfoBox,
  Warning,
  RelatedTopics,
  FaqAccordion,
  WorkflowCard,
} from '@site/src/components';

# Identity Verification

**Identity Verification** lets Business Actions declare how much trust they need. Qefro first **resolves identity** from the channel, then asks the organization's **Customer Identity Service** whether the customer may perform the action.

Qefro is not a customer Identity Provider — it never sends or verifies OTP.

## Architecture

```text
Channel → Identity Resolver → Resolved Identity → Organization Customer Identity Service → Authentication Context → Business Tool
```

## Authentication requirements

| Requirement | Meaning |
| --- | --- |
| `public` | Anyone can execute |
| `verified_channel` | Requires channel-verified identity (WhatsApp phone, website session, etc.) |
| `organization_challenge` | Requires the organization's Customer Identity Service |

Configure on each Business Action:

```json
{ "required_auth_level": "verified_channel" }
```

Customer authentication is configured under **Organization → Customer Identity Service**, not on individual Business Tools.

## Design philosophy

- Do **not** force OTP or login unless the organization sets `organization_challenge`.
- WhatsApp Meta-verified phone **is** verified channel identity — no OTP for `verified_channel`.
- Website Widget JWT / session from `identify()` **is** verified channel identity — no OTP for `verified_channel`.
- Qefro orchestrates organization challenges; your systems remain the authority for access to your APIs (see [Identity Forwarding](/docs/platform/identity-forwarding)).

## Channel identity

| Channel | Verified when | Identity type |
| --- | --- | --- |
| WhatsApp | Meta-verified phone present | `phone_number` |
| Website Widget | Host JWT or session via `identify()` | `jwt` / `session` |
| Internal Portal | Employee login (tools blocked in V1) | — |

If a tool requires `verified_channel` and the channel has no verified identity, the runtime returns **`CHANNEL_IDENTITY_REQUIRED`** (widget: ask the customer to sign in on your site). Qefro does not start OTP.

## Organization challenge

Only when:

```json
{ "required_auth_level": "organization_challenge" }
```

Qefro calls your **Customer Identity Service** (`POST /identity`) with the resolved identity. Your service may return `authenticated`, `challenge_required` (OTP / login URL), `denied`, or `customer_not_found`.

When a challenge is required, Qefro relays your message to the customer. When they reply, Qefro **forwards** the reply to your Identity API — your organization verifies it. Qefro never validates OTP codes.

A valid Authentication Context from your service is reused until it expires.

## WhatsApp — order status (verified channel)

<WorkflowCard title="verified_channel on WhatsApp" steps={[
  {title: 'User messages', description: 'Where is my order? Meta-verified phone → verified_channel.'},
  {title: 'Policy check', description: 'Tool requires verified_channel → Allow immediately.'},
  {title: 'Execute', description: 'Business Tool runs with X-Qefro-* headers. No organization challenge.'},
]} />

## WhatsApp — download invoice (organization challenge)

<WorkflowCard title="organization_challenge on WhatsApp" steps={[
  {title: 'User messages', description: 'Download my invoice (phone already verified).'},
  {title: 'Identity API', description: 'Qefro POSTs resolved phone identity to your Customer Identity Service.'},
  {title: 'Challenge', description: 'Your service sends OTP / asks for login; Qefro relays the message.'},
  {title: 'User replies', description: 'Qefro forwards the reply; your service verifies and returns a credential.'},
  {title: 'Execute', description: 'Runtime injects Authentication Context into the Business Tool request.'},
]} />

## Phone mappings

Local table (Admin API) for enriching WhatsApp identity:

- `PUT /api/v1/workspaces/:id/phone-mappings` — upsert `{ phone, customer_id?, email?, display_name? }`
- `GET /api/v1/workspaces/:id/phone-mappings`
- `DELETE /api/v1/workspaces/:id/phone-mappings/:id`

## Widget endpoints

- `identify()` JWT/session counts as **`verified_channel`**
- `POST /api/v1/widget/identity/challenge` and `…/verify` forward to your Customer Identity Service (Qefro does not send or verify OTP)

## Identity forwarding

Outbound Business Tool requests receive:

| Header | Example |
| --- | --- |
| `X-Qefro-User-ID` | customer / user id |
| `X-Qefro-Organization` | tenant UUID |
| `X-Qefro-Authentication-Level` | `public` / `verified_channel` / `organization_challenge` |
| `X-Qefro-Channel` | `whatsapp` / `widget` / `portal` |
| `X-Qefro-Phone` | E.164 when known |

Plus org-issued Bearer / JWT / Cookie credentials when your Identity API returns them. Tokens are never sent to the LLM.

## Security notes

<Warning>
Qefro never stores customer OTP codes. Organization-issued Authentication Context credentials are encrypted at rest, never logged, and never exposed to the AI model. Never trust client-asserted identity without channel validation.
</Warning>

<InfoBox>
Authentication ≠ authorization. After the auth gate, tool preconditions and workspace RBAC still apply. Internal Portal Business Tool execution remains blocked in V1.
</InfoBox>

## Related

<RelatedTopics topics={[
  {label: 'Identity & Authentication', to: '/docs/platform/identity-and-authentication'},
  {label: 'Business Tools', to: '/docs/platform/business-tools'},
  {label: 'Business Actions', to: '/docs/platform/business-actions'},
  {label: 'WhatsApp', to: '/docs/platform/whatsapp'},
  {label: 'Identity Forwarding', to: '/docs/platform/identity-forwarding'},
]} />

<FaqAccordion items={[
  {question: 'Do Business Actions need WhatsApp-specific code?', answer: 'No. Declare required_auth_level only; adapters resolve identity.'},
  {question: 'Does WhatsApp trigger OTP for order status?', answer: 'Not if the tool is verified_channel. Meta-verified phone is enough. Organization challenges are owned by your Customer Identity Service.'},
  {question: 'Who sends the OTP?', answer: 'Your organization. Qefro only relays messages and forwards replies to your Identity API.'},
  {question: 'Does verified_channel unlock organization_challenge tools?', answer: 'No. Those tools need a successful response from your Customer Identity Service.'},
  {question: 'Can the Internal Portal execute Business Tools?', answer: 'Not in V1. The channel gate blocks Portal before auth and HTTPS.'},
]} />
